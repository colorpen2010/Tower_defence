import pygame,model
screen=pygame.display.set_mode([800,800])
def risovanie():
    pygame.display.flip()
    screen.blit(model.background1,[0,0])


def map_generation(image:pygame.Surface,map_size):
    msize=image.get_size()
    if
    screen.blit(model.background1,[,0])

